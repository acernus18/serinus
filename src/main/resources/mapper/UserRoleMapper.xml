<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.maples.serinus.repository.UserRoleMapper">
    <resultMap id="BaseResultMap" type="org.maples.serinus.model.UserRole">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="role_id" jdbcType="INTEGER" property="roleId"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        select `id`, `user_id`, `role_id`, `status`
        from tb_serinus_user_role
        <where>
            `user_id` = ${userId}
        </where>
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        select `id`, `user_id`, `role_id`, `status`
        from tb_serinus_user_role
        <where>
            `role_id` = ${roleId}
        </where>
    </select>

    <select id="selectOneByUserIdAndRoleId" resultMap="BaseResultMap">
        select `id`, `user_id`, `role_id`, `status`
        from tb_serinus_user_role
        <where>
            `user_id` = ${userId} and `role_id` = ${roleId}
        </where>
    </select>
</mapper>