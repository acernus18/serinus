<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.maples.serinus.repository.RoleResourcesMapper">
    <resultMap id="BaseResultMap" type="org.maples.serinus.model.RoleResources">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="role_id" jdbcType="INTEGER" property="roleId"/>
        <result column="resources_id" jdbcType="INTEGER" property="resourcesId"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="selectByRoleID" resultMap="BaseResultMap">
        select id, role_id, resources_id, `status`, create_time, update_time
        from tb_serinus_role_resources
        where role_id = ${roleID}
    </select>

    <select id="selectByRoleIDAndResourceID" resultMap="BaseResultMap">
        select id, role_id, resources_id, `status`, create_time, update_time
        from tb_serinus_role_resources
        where role_id = ${roleID} and resources_id = ${resourcesID}
    </select>
</mapper>